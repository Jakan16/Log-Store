apiVersion: apps/v1
kind: Deployment
metadata:
  name: "log-store"
spec:
  selector:
    matchLabels:
      app: "log-store"
  template:
    metadata:
      labels:
        app: "log-store"
    spec:
      containers:
        - name: "log-store"
          image: "gcr.io/log-store/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "log-store"
spec:
  selector:
    app: "log-store"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080