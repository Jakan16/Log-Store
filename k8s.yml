apiVersion: apps/v1
kind: Deployment
metadata:
  name: "logstore"
spec:
  selector:
    matchLabels:
      app: "logstore"
  template:
    metadata:
      labels:
        app: "logstore"
    spec:
      containers:
        - name: "logstore"
          image: "gcr.io/logstore/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "logstore"
spec:
  selector:
    app: "logstore"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080